const adviceComponent = (props: {id: string, advice: string[]}) => {
    return (
      <div>
          <h1>{props.id}</h1>
          {
        
          }
          <hr />
      </div>
    )
  }
  

  export {adviceComponent}